<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SpacePortal.Views.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SpacePortal.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="LoginWindow">

    <Grid>
        <Grid x:Name="ContentArea">
            <Grid.Background>
                <ImageBrush ImageSource="/Assets/LoginBackground.png" Stretch="UniformToFill"></ImageBrush>
            </Grid.Background>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*" />
                <ColumnDefinition Width="5*" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Frame x:Name="LoginFrame" Grid.Row="0" Grid.Column="1" Margin="0,50,30,50" CornerRadius="20" Background="White" BorderBrush="Black" >

            </Frame>
        </Grid>
        <Grid x:Name="LoadingOverlayGrid" Background="#99000000" Visibility="Collapsed"/>
        <ProgressRing x:Name="LoadingOverlay" Width="64" Height="64" Visibility="Collapsed" IsActive="True"/>
    </Grid>
</Window>
