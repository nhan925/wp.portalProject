<Page
    x:Class="SpacePortal.Views.GradesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:syncfuion="using:Syncfusion.UI.Xaml.DataGrid"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <Page.Resources>
        <Style x:Key="MicaStyle" TargetType="StackPanel">
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}"/>
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
        </Style>
        <Style x:Key="HorizontalStackPanel" TargetType="StackPanel">
            <Setter Property="Orientation" Value="Horizontal"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Width" Value="Auto"/>
            <Setter Property="Height" Value="Auto"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Width" Value="128"/>
            <Setter Property="Height" Value="32"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Width" Value="128"/>
            <Setter Property="Height" Value="32"/>
        </Style>
    </Page.Resources>

    <Grid x:Name="ContentArea">
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="12*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--ComboBoxes and Button-->
        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Spacing="40" Margin="36,0,0,0" >
            <ComboBox x:Name="ComboBoxYear" x:Uid="GradesPage_ComboBoxYear"/>
            <ComboBox x:Name="ComboBoxTerm" x:Uid="GradesPage_ComboBoxSemester"/>
            <Button x:Uid="GradesPage_ButtonShowGradesPage" Style="{StaticResource AccentButtonStyle}"/>
        </StackPanel>
                
        <!--Button and TeachingTip-->
        <StackPanel Grid.Row="0" Style="{StaticResource HorizontalStackPanel}" HorizontalAlignment="Right"  Margin="0,0,36,0" >
            <Button x:Uid="GradesPage_ButtonRequest" Style="{StaticResource AccentButtonStyle}"/>
            <TeachingTip x:Name="ToggleThemeTeachingTip2"
                Title="Yeu cau"
                Subtitle="Chon yeu cau ban muon thuc hien"
                PreferredPlacement="Auto"
                PlacementMargin="20"
                IsLightDismissEnabled="True"
                ActionButtonClick="ToggleThemeTeachingTip2_ActionButtonClick"
                ActionButtonContent="Toggle theme now"
                CloseButtonContent="Got it!">
            </TeachingTip>
        </StackPanel>
        
        <!--General Information-->
        <StackPanel Grid.Row="1" Orientation ="Horizontal" Style="{StaticResource MicaStyle}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="36,0,36,12" Spacing="200">
            <StackPanel Style="{StaticResource HorizontalStackPanel}" Margin="71,30,71,30" Height="36">
                <TextBlock Text="GPA" Foreground="Black"  FontSize="24" Opacity="0.5" Margin="12,0,50,0"/>
                <StackPanel Style="{StaticResource HorizontalStackPanel}" HorizontalAlignment="Center" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" Padding="8">
                    <StackPanel Style="{StaticResource HorizontalStackPanel}" Margin="0,0,100,0">
                        <TextBlock x:Uid="GradesPage_ScaleGrade4"  Foreground="Black" Opacity="0.4"/>
                        <TextBlock Text="3.5" Foreground="Black" Opacity="0.4" Margin="20,0,0,0"/>
                    </StackPanel>
                    <StackPanel Style="{StaticResource HorizontalStackPanel}" >
                        <TextBlock x:Uid="GradesPage_ScaleGrade10" Foreground="Black" Opacity="0.4"/>
                        <TextBlock Text="8.5" Foreground="Black" Opacity="0.4"  Margin="20,0,0,0" />
                    </StackPanel>
                </StackPanel>
            </StackPanel>

            <StackPanel  Style="{StaticResource HorizontalStackPanel}">
                <TextBlock x:Uid="GradesPage_NumberOfCourses" Foreground="Black" FontSize="24" Opacity="0.5"/>
                <TextBlock Text="5" Foreground="Black" FontSize="24" Opacity="0.5" Margin="20,0,0,0"/>
            </StackPanel>

            <StackPanel  Style="{StaticResource HorizontalStackPanel}">
                <TextBlock x:Uid="GradesPage_NumberOfCredits"  Foreground="Black" FontSize="24" Opacity="0.5"/>
                <TextBlock Text="50" Foreground="Black" FontSize="24" Opacity="0.5" Margin="20,0,0,0"/>
            </StackPanel>
        </StackPanel>

        
        <!--DataGrid-->
        <StackPanel Grid.Row="2" Orientation ="Horizontal" Style="{StaticResource MicaStyle}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="36,0,36,12" Spacing="200">
            <!--<ListView  Name="employeesComboBox" Width="500" Height="500" 
                ItemsSource="{x:Bind ViewModel.Grades}">
            </ListView>-->
            
            <!--Advanced: wait for community license request is approved-->
            <Grid x:Name="rootGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <syncfuion:SfDataGrid x:Name="sfDataGrid"
                                      AutoGenerateColumns="True"
                                      ItemsSource="{x:Bind ViewModel.Grades}"
                                      HorizontalAlignment="Stretch"
                                      VerticalAlignment="Stretch"/>
            </Grid>
            
            
        </StackPanel>
    </Grid>
</Page>
